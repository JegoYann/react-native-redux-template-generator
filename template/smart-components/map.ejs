'use strict';

import React, { Component } from 'react';

import { connect } from 'react-redux';
import { i18n } from '../locales/i18n';
import { toJS } from '../utils/immutable_to_js';
import { navigateTo } from '../actions/navigation';
<%- "import Dumb from '../dumb-components/"+key+"';" %>
// TODO Import your own action creators
// import { fetchAddresses, selectFromLocation } from '../actions/actions_delivery';


const mapStateToProps = (state) => {
  return {
    labels: {
      <% labels.forEach(label => { %>
      <%- label.key+": i18n('"+label.value+"')," %>
      <% }); %>
    },
    flags: {
    },
    // TODO Add your prediction state
    // predictions: state.getIn(['map', 'predictions']) || [],
    predictions: [], 
    mapKey: <%- "'"+mapKey+"'" %> || "0",
  }
}

const mapDispatchToProps = (dispatch) => {
  return {
    fetchAddresses: (value, mapKey) => {
      // TODO Dispatch your own action
      // dispatch(fetchAddresses(value, mapKey));
    },
    selectLocation: (location) => {
      // TODO Dispatch your own action
      // dispatch(selectFromLocation(location));
      // dispatch(navigateTo('deliveryTo'));
    }
  }
}

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(toJS(Dumb))
